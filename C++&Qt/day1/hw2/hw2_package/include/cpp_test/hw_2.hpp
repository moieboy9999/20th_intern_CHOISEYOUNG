#include <iostream>
#include <ctime>

using namespace std;

struct cold//구조체(구조체는 신이다)
{
    int X;
    int Y;
};

class pross{
    private:

        float mid,mad;

    public:
        float input();
        int muld(int a);
        double sqrt(int a);

};
